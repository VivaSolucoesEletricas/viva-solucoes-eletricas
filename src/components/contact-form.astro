<form class="grid bg-white grid-cols-1 gap-3 max-w-sm rounded p-6">
  <p class="font-bold text-viva-orange">
    Quero entender melhor como posso economizar na minha conta de energia!
  </p>
  <input
    type="text"
    name="name"
    placeholder="Nome Completo"
    class="px-2 py-1 border border-gray-300 rounded"
    required
  />
  <input
    type="email"
    name="email"
    placeholder="Endereço de E-mail"
    class="px-2 py-1 border border-gray-300 rounded"
    required
  />
  <input
    type="tel"
    name="phone"
    placeholder="Seu telefone / WhatsApp"
    class="px-2 py-1 border border-gray-300 rounded"
    required
  />
  <input
    type="text"
    name="energyConsumption"
    placeholder="Valor da ultima conta de energia"
    class="px-2 py-1 border border-gray-300 rounded"
    required
  />
  <button type="submit" class="bg-viva-orange h-10 text-white rounded">
    Solicitar contato
  </button>
</form>

<script>
  // import { actions } from "astro:actions";
  const form = document.querySelector("form");

  form?.addEventListener("submit", async (event) => {
    event.preventDefault();
    const formData = new FormData(form);
    console.log(Object.fromEntries(formData));
    // await actions.sendEmail(formData);
    // await actions.saveToSheets(formData);
  });
</script>
